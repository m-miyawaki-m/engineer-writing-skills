---
title: "実装時のログ（デバッグログ）"
tags: ["用語"]
created: "2026-02-18"
updated: "2026-02-26"
---

## 意味

開発・デバッグ目的で一時的に埋め込むログ。処理の流れや変数の中身を確認するために使い、本番リリース前に削除またはログレベルで制御する。`console.log` の残骸は技術負債になる

## 具体例

【実装時ログの正しい使い方】
```javascript
// デバッグ時: 処理の流れと変数を確認
console.log('=== authenticate start ===', { email });
console.log('user found:', user);
console.log('bcrypt result:', isMatch);
console.log('token generated:', token);
```

【リリース前に必ずやること】
1. `console.log` を全て削除 or `logger.debug()` に置換
2. 機密情報（パスワード、トークン）がログに出ていないか確認
3. Lintルール `no-console` で自動検出

## アンチパターン

`console.log('ここに来た')` `console.log('test')` `console.log(data)` が本番コードに残っている。パスワードや個人情報がログに出力されている

## 使いどころ

開発時のデバッグ、コードレビューでのログ残骸チェック
